public int binaryZeros(int n) {
    int count = 0;
    String binaryRep = Integer.toBinaryString(n);

    for (int i = 0; i < binaryRep.length(); i++) {
        if (binaryRep.substring(i,i+1).equals("0")) { count ++; }
    }
    
    return count;
    
}
